# --- CONFIGURATION LOCALE (PROFIL LOCAL) ---
# Copier ce fichier vers application-local.properties et remplir vos identifiants.
# Lancer avec : mvn spring-boot:run -Dspring-boot.run.profiles=local

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/platmoi}
spring.datasource.username=${DB_USERNAME:platmoi}
spring.datasource.password=${DB_PASSWORD:platmoi}

spring.jpa.show-sql=true